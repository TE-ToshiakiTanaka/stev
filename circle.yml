machine:
    python:
        version: 2.7
    timezone: Asia/Tokyo

dependencies:
    pre:
        - pip install wheel

test:
    override:
        - python setup.py test
        - python setup.py bdist_wheel

general:
  artifacts:
    - "~/dist"
